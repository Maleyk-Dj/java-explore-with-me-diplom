server:
  port: 8080

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

      routes:
        - id: ewm-service-route
          uri: lb://ewm-service
          predicates:
            - Path=/api/**
